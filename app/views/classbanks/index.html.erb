<h2> <%= @name %>'s Classbanks</h2>

<div>
  <% if @classbanks %>
    <% @classbanks.each do |classbank| %>
      <div>
        <%= link_to classbank.name, classbank %>
      </div>
    <% end  %>
  <% end  %>
</div>
